// Root "/" — o middleware redireciona para /teacher/dashboard ou /student/dashboard
// Este componente é renderizado apenas por milissegundos durante o redirect
export default function RootPage() {
  return null
}
